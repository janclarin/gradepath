<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Holo.Light">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/CustomActionBar</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- ActionBar style -->
    <style name="CustomActionBar" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:height">48dp</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:background">@color/theme_primary_color</item>
        <item name="android:backgroundStacked">@android:color/transparent</item>
    </style>

    <!-- ActionBar tabs text style -->
    <style name="CustomActionBarTabText" parent="@android:style/Widget.Holo.Light.ActionBar.TabText">
        <item name="android:textColor">@android:color/primary_text_dark</item>
    </style>

    <!-- Borderless button for empty view for list fragments -->
    <style name="emptyListTextView" parent="@android:style/Widget.Holo.Button.Borderless">
        <item name="android:padding">8dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/tertiary_text_light</item>
        <item name="android:textSize">34sp</item>
        <item name="android:fontFamily">sans-serif-thin</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
        <item name="android:visibility">gone</item>
    </style>

    <!-- Borderless button for list items -->
    <style name="listItemButton" parent="@android:style/Widget.Holo.Button.Borderless">
        <item name="android:layout_width">40dp</item>
        <item name="android:layout_height">64dp</item>
        <item name="android:focusable">false</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

    <!-- Check box -->
    <style name="checkBoxDialog">
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@android:color/secondary_text_light_nodisable</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Category title text view -->
    <style name="headerTextView">
        <item name="android:gravity">center_vertical|left</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@android:color/secondary_text_light_nodisable</item>
    </style>

    <!-- Spinner-style button -->
    <style name="spinnerButton" parent="@android:style/Widget.Holo.Light.Spinner">
        <item name="android:layout_height">40dp</item>
        <item name="android:enabled">true</item>
        <item name="android:paddingLeft">12dp</item>
    </style>

    <style name="dialogPrompt">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:layout_marginBottom">48dp</item>
        <item name="android:paddingBottom">32dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:paddingTop">8dp</item>
    </style>

    <!-- List view style for cards -->
    <style name="cardListView">
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:paddingBottom">72dp</item>
        <item name="android:divider">@null</item>
        <item name="android:dividerHeight">8dp</item>
        <item name="android:clipToPadding">false</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:drawSelectorOnTop">true</item>
    </style>

    <style name="cardView">
        <item name="android:background">@drawable/bg_card</item>
    </style>

</resources>
